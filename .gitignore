# Sui Move
contracts/build/
contracts/.move/

/research

# Generated by scripts/deploy.sh — machine-specific, not secret but not shared
.deployed.env

# TypeScript / Node
node_modules/
dist/
*.tsbuildinfo
.vercel/

# Generated at build time from root plugins/ — single source of truth lives there
packages/cli/plugins/
# Built visualizer bundled into the CLI at publish time
packages/cli/ui/

# Secrets — never commit
.env
.env.local
.env.*.local
.env.test
.env.production
*.key
# .env.example is intentionally NOT ignored (safe template, no secrets)

# Config generated by memfork init — keep local, do not commit
.memfork/
.memfork/credentials.json
.memfork/*.local.json

# OS
.DS_Store
SPIKES.md
CHECKPOINT.md

# Full README with stack details — swap in before submission
README.full.md
landing.html
.vercel
