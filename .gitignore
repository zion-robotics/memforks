# Sui Move
contracts/build/
contracts/.move/
/research

# TypeScript / Node
node_modules/
dist/
*.tsbuildinfo

# Secrets — never commit
.env
.env.local
.env.*.local

# Config generated by memfork init (safe to commit .memforks.json, not .env.local)
spikes/.env.local

# OS
.DS_Store
