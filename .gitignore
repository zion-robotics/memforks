# Sui Move
contracts/build/
contracts/.move/

/research

# Generated by scripts/deploy.sh — machine-specific, not secret but not shared
.deployed.env

# TypeScript / Node
node_modules/
dist/
*.tsbuildinfo

# Secrets — never commit
.env
.env.local
.env.*.local

# Config generated by memfork init
# .memfork/config.json is safe to commit (treeId, network — no secrets)
# .memfork/credentials.json must never be committed (private keys)
.memfork/credentials.json
.memfork/*.local.json
spikes/.env.local

# OS
.DS_Store
SPIKES.md
CHECKPOINT.md

# Full README with stack details — swap in before submission
README.full.md
landing.html
