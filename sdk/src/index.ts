/**
 * @memfork/core — public API surface.
 * Phase 0: types + namespace helper only.
 * Phase 1 adds: MemoryClient, resolvers, perms builders, event subscriptions.
 */

export * from "./types.js";

// Phase 1 exports:
// export { MemoryClient } from "./client.js";
// export { resolvers } from "./resolvers.js";
// export { perms } from "./perms.js";
