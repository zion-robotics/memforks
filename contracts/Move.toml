[package]
name = "memforks"
version = "0.1.0"
edition = "2024.beta"
license = "Apache-2.0"
authors = ["MemForks contributors"]

[dependencies]
# Sui framework is auto-added by the CLI; no explicit dep needed.

[addresses]
memforks = "0x0"

[dev-dependencies]

[dev-addresses]
